#pragma once

#include <domain_models/base_dm.hpp>

namespace domain_models
{
    class reports_dm : public base_dm
    {
        public:
        reports_dm();
        ~reports_dm() = default;

        private:
    };
}